# AW12 Циферблаты — Кастомное приложение для часов AW12

## Что это?
Android-приложение для установки кастомных циферблатов на смарт-часы AW12 (и другие часы на прошивке MoYoung / Da Fit).

Использует **официальный Flutter-плагин от MoYoung** (`moyoung_ble_plugin`) — тот же BLE-протокол, что и оригинальное приложение Da Fit.

## Возможности
- 🔍 Сканирование и подключение к часам AW12 по Bluetooth
- 🔄 Переключение между встроенными циферблатами
- 🖼️ Загрузка своей картинки как циферблата (из галереи или камеры)
- 🔋 Отображение уровня заряда часов

## Как собрать APK

### 1. Установи Flutter

Если Flutter ещё не установлен:

1. Открой Android Studio → **Settings → Plugins → Marketplace**
2. Найди **Flutter** и установи (он подтянет Dart автоматически)
3. Перезапусти Android Studio
4. Скачай Flutter SDK: https://docs.flutter.dev/get-started/install
5. Укажи путь к Flutter SDK в **Settings → Languages & Frameworks → Flutter**

### 2. Открой проект

1. Открой Android Studio
2. **File → Open** → выбери папку `aw12_watchface_app`
3. Дождись пока Gradle и pub синхронизируются

### 3. Установи зависимости

В терминале Android Studio выполни:
```bash
flutter pub get
```

### 4. Собери APK

```bash
flutter build apk --release
```

Готовый APK будет в:
```
build/app/outputs/flutter-apk/app-release.apk
```

### 5. Отправь брату

Отправь файл `app-release.apk` брату. Он устанавливает APK, открывает, ищет часы, подключается, и выбирает циферблат.

## Структура проекта

```
lib/
├── main.dart                          # Точка входа
├── screens/
│   ├── home_screen.dart               # Экран сканирования и подключения
│   └── watchface_gallery_screen.dart  # Галерея циферблатов
└── services/
    └── ble_service.dart               # BLE-сервис (обёртка над moyoung_ble_plugin)
```

## Требования
- Android 5.0+ (minSdk 18)
- Bluetooth 4.0+ (BLE)
- Часы AW12 или другие на прошивке MoYoung v2

## Возможные проблемы

**Часы не находятся при сканировании:**
- Убедитесь что Bluetooth включён
- Дайте приложению разрешения на геолокацию (Android требует это для BLE)
- Отключите часы от Da Fit (часы могут быть подключены только к одному приложению)

**Ошибка при загрузке циферблата:**
- Убедитесь что изображение не слишком большое (рекомендуется 240×240)
- Часы должны быть подключены и не в режиме сна

## Дальнейшее развитие
- [ ] Добавить предустановленные дизайны циферблатов
- [ ] Редактор циферблатов (выбор положения времени, цвета шрифта)
- [ ] Загрузка полноценных бинарных циферблатов (с шагами, пульсом и т.д.)
- [ ] Уведомления и настройки часов
